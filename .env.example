# DOCKER ENV - Example file
# Copy this file as .env and fill in the appropriate values

REGISTRY_USER=your_dockerhub_user  # Docker Hub username (e.g., myusername)
IMAGE_NAME=your_image_name         # Name of the Docker image (e.g., myapp)
TAG=latest                         # Image tag (e.g., latest, v1.0.0)
SERVER_USER=your_ssh_user           # SSH user on the VPS (e.g., debian, root)
SERVER_HOST=your_server_ip_or_host  # VPS IP address or hostname (e.g., vps-xxxx.xxxx.net)
CONTAINER_NAME=your_container_name  # Name of the running Docker container (e.g., myapp_container)
DOCKER_PORTS="-p 3000:3000"         # Port mapping (e.g., "-p 80:3000" to expose it on HTTP)
DOCKER_ADDITIONAL_ARGS=""           # Additional arguments for Docker (e.g., "-e NODE_ENV=production")
DOCKERFILE_PATH="/path/to/Dockerfile"  # Path to the Dockerfile on the local machine
